#if !SFX_ENUM_GENERATED
namespace Cu1uSFX
{
    // Fallback enum that is only compiled if the sfx enum has not been generated yet.
    public enum SFX
    {
        None = 0
    }
}
#else
namespace Cu1uSFX.Internal
{
    // Fallback enum that is only compiled if the sfx enum has not been generated yet.
    public enum SFX
    {
        None = 0
    }
}
#endif